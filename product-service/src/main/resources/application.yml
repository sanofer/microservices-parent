spring:
  data:
    mongodb:
      database:product
      host:product-db
      port:27017
  application:
    name: product-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
